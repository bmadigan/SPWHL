
= simple_form_for @team do |f| 
  = f.error_messages

  = f.input :name, :label => 'Team Name'
  = f.association :league, :include_blank => false
  = f.input :manager_id
  = f.input :team_page_content,  :as => :text, :label => 'Enter the content for this teams homepage'
  = f.button :submit
